# ClipEarn Hub Landing Page

## Preview locally

From the repository root, run a static server and open the page in your browser:

```bash
python -m http.server 8000
```

Then visit:

```
http://127.0.0.1:8000/
```

If you prefer another port, replace `8000` in both the command and URL.
